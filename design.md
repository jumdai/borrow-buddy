# Borrow Buddy: เอกสารออกแบบ (เวอร์ชัน 2)

ศัพท์ทั้งหมดในเอกสารนี้อ้างอิงจาก [CONTEXT.md](./CONTEXT.md)

## 1. ภาพรวมและเป้าหมาย
เว็บหน้าเดียวสำหรับ **เจ้าของ** ใช้บันทึกว่า **เพื่อน** ยืม **ของ** อะไร เมื่อไร ต้องคืนเมื่อไร และกดว่า **คืนแล้ว** ได้

**สิ่งที่เปลี่ยนจากเวอร์ชัน 1**
- ย้ายที่เก็บ Loan จาก localStorage ไป Supabase (Postgres) ใช้ได้จากหลายเครื่อง
- เจ้าของต้อง **เข้าสู่ระบบ** ด้วยอีเมล + รหัสผ่าน ไม่มีการสมัครสมาชิกเอง
- ใช้ Row Level Security (RLS) ให้เจ้าของเห็นและแก้ได้เฉพาะ Loan ของตัวเอง
- **นำเข้าข้อมูลเดิม** จาก localStorage ได้ครั้งเดียว
- กติกา สถานะ และหน้าจอรายการ Loan เหมือนเวอร์ชัน 1 ทุกอย่าง

## 2. ขอบเขต

**อยู่ในขอบเขต**
- เข้าสู่ระบบ / ออกจากระบบ ด้วยอีเมล + รหัสผ่าน
- เพิ่ม Loan, แก้ไข Loan, ดูรายการ Loan (เฉพาะของตัวเอง)
- กดคืนแล้ว (ระบุวันที่คืนจริง) และยกเลิกการคืน
- ค้นหา/กรองตามชื่อเพื่อน
- แสดงสถานะ และจำนวนวันเกินกำหนด
- นำเข้าข้อมูลเดิมจาก localStorage ของเครื่องนั้นครั้งเดียว

**นอกขอบเขต (เวอร์ชัน 2)**
- สมัครสมาชิกเอง, ลืมรหัสผ่าน/รีเซ็ตรหัสผ่านผ่านหน้าเว็บ, ล็อกอินผ่าน Google/Magic Link
- เพื่อนเข้าเว็บ, การแชร์ Loan ระหว่างเจ้าของ
- ลบ Loan (ทั้งในหน้าเว็บและระดับฐานข้อมูล), แจ้งเตือน, เลื่อนกำหนดคืน, รูปภาพของ
- ทะเบียนรายชื่อเพื่อน/ของแยก, การตรวจของซ้ำซ้อน
- ใช้งานแบบออฟไลน์, อัปเดตแบบเรียลไทม์ข้ามเครื่อง, ส่งออกไฟล์

## 3. เทคโนโลยี
- React + Vite, ภาษา JavaScript (ไม่ใช้ TypeScript)
- Supabase: Auth (อีเมล + รหัสผ่าน) และ Postgres ผ่านไลบรารี `@supabase/supabase-js`
- ทดสอบด้วย Vitest
- ค่าตั้งค่าอ่านจาก `.env.local` (ไม่ commit):
  - `VITE_SUPABASE_URL`
  - `VITE_SUPABASE_PUBLISHABLE_KEY` (คีย์สาธารณะเท่านั้น ห้ามใช้ service role key ฝั่งเว็บ)
- มีไฟล์ `.env.example` เป็นตัวอย่างชื่อตัวแปร (ไม่มีค่าจริง)

## 4. โมเดลข้อมูล

**Loan (ในแอป)** รูปแบบเดิม เพิ่มเจ้าของ
| ฟิลด์ | ชนิด | หมายเหตุ |
|---|---|---|
| `id` | string (uuid) | ฐานข้อมูลสร้างให้ |
| `friendName` | string | ชื่อเล่นของเพื่อน พิมพ์เองทุกครั้ง ห้ามว่าง |
| `itemName` | string | ชื่อของ ห้ามว่าง |
| `borrowedDate` | string `YYYY-MM-DD` | วันที่ยืม |
| `dueDate` | string `YYYY-MM-DD` | กำหนดคืน บังคับกรอก |
| `returnedDate` | string `YYYY-MM-DD` หรือ `null` | วันที่คืนจริง ว่างจนกว่าจะกดคืน |

**ตาราง `public.loans` (ใน Supabase)**
| คอลัมน์ | ชนิด | หมายเหตุ |
|---|---|---|
| `id` | `uuid` PK | `default gen_random_uuid()` |
| `owner_id` | `uuid` not null | อ้างอิง `auth.users(id)`, `default auth.uid()` |
| `friend_name` | `text` not null | `check (length(trim(friend_name)) > 0)` |
| `item_name` | `text` not null | `check (length(trim(item_name)) > 0)` |
| `borrowed_date` | `date` not null | |
| `due_date` | `date` not null | `check (due_date >= borrowed_date)` |
| `returned_date` | `date` null | `check (returned_date is null or returned_date >= borrowed_date)` |
| `created_at` | `timestamptz` not null | `default now()` |
| `updated_at` | `timestamptz` not null | `default now()` อัปเดตด้วย trigger |

- ดัชนีบน `owner_id`
- แปลงชื่อฟิลด์ camelCase ↔ snake_case ในโค้ดฝั่งเว็บที่เดียว (`loanMapper.js`)
- `owner_id` ไม่อยู่ใน Loan ของแอป และฝั่งเว็บไม่ส่งค่านี้เอง ให้ฐานข้อมูลใส่จาก `auth.uid()`
- ใช้ชนิด `date` (ไม่มีเวลา/โซนเวลา) เพื่อไม่ให้วันเลื่อน

วันที่เก็บเป็น ค.ศ. และแสดงผลเป็น พ.ศ. เท่านั้น (เช่น 24 ก.ย. 2569) สถานะและจำนวนวันเกินกำหนดไม่เก็บ คำนวณจากวันที่ทุกครั้ง

## 5. กติกา

**สถานะ** (คำนวณจากวันนี้ ตามเวลาเครื่องของเจ้าของ)
- **คืนแล้ว**: `returnedDate` ไม่เป็น null
- **เกินกำหนด**: ยังไม่คืน และวันนี้ > `dueDate`
- **ยังไม่คืน**: ยังไม่คืน และวันนี้ ≤ `dueDate` (ถึงกำหนดวันนี้พอดียังไม่เกิน)

**จำนวนวันเกินกำหนด** = วันนี้ − `dueDate` (นับเป็นวัน ใช้กับ Loan ที่เกินกำหนดเท่านั้น)

**การตรวจวันที่** (ตรวจที่ฝั่งเว็บก่อนส่ง และฐานข้อมูลมี check constraint ซ้ำอีกชั้น)
- `dueDate` ต้องไม่ก่อน `borrowedDate`
- `returnedDate` ต้องไม่ก่อน `borrowedDate`
- ชื่อเพื่อนและชื่อของห้ามว่าง
- ถ้าฐานข้อมูลปฏิเสธ ให้แสดงข้อความภาษาไทยทั่วไป ไม่แสดงข้อความดิบจากฐานข้อมูล

**การกดคืนแล้ว**
- ค่าเริ่มต้นของวันที่คืนจริงคือวันนี้ แก้เป็นวันย้อนหลังได้
- ยกเลิกการคืนได้ (ตั้ง `returnedDate` กลับเป็น null)

## 6. การยืนยันตัวตนและสิทธิ์

**บัญชีเจ้าของ**
- ปิด "Allow new users to sign up" ใน Supabase Auth
- สร้างบัญชีเจ้าของด้วยมือใน Supabase Dashboard (Authentication → Users) พร้อมยืนยันอีเมลแล้ว
- หน้าเว็บไม่มีปุ่มหรือฟอร์มสมัครสมาชิก

**เข้าสู่ระบบ / ออกจากระบบ**
- ใช้ `signInWithPassword` และ `signOut`
- supabase-js จำ session ในเบราว์เซอร์ เปิดหน้าใหม่ไม่ต้องล็อกอินซ้ำจนกว่าจะออกจากระบบหรือ session หมดอายุ
- ติดตาม `onAuthStateChange` เมื่อ session หายให้ล้าง Loan ออกจาก state และกลับไปหน้าเข้าสู่ระบบ
- ล็อกอินไม่สำเร็จ แสดง "อีเมลหรือรหัสผ่านไม่ถูกต้อง" (ไม่บอกว่าผิดช่องไหน)

**RLS บนตาราง `loans`**
- เปิด RLS (`enable row level security`)
- policy ทั้งหมดใช้กับ role `authenticated` เท่านั้น ไม่มี policy สำหรับ `anon`
  - `select`: `using ((select auth.uid()) = owner_id)`
  - `insert`: `with check ((select auth.uid()) = owner_id)`
  - `update`: `using ((select auth.uid()) = owner_id)` และ `with check ((select auth.uid()) = owner_id)` (ย้าย Loan ไปเจ้าของอื่นไม่ได้)
  - **ไม่มี policy `delete`** และ `revoke delete on public.loans from anon, authenticated` เพื่อกันการลบจาก API
- ตรวจด้วย Supabase advisors (security) หลังสร้างตาราง ต้องไม่มีคำเตือนเรื่อง RLS

## 7. หน้าจอ (หน้าเดียว)

**ยังไม่เข้าสู่ระบบ**
- **ฟอร์มเข้าสู่ระบบ**: อีเมล, รหัสผ่าน, ปุ่มเข้าสู่ระบบ ระหว่างรอให้ปุ่มกดซ้ำไม่ได้
- ไม่แสดงข้อมูล Loan ใดๆ

**เข้าสู่ระบบแล้ว** (เหมือนเวอร์ชัน 1 เพิ่มส่วนหัว)
- **ส่วนหัว**: อีเมลเจ้าของ, ปุ่มออกจากระบบ, ปุ่มสลับโหมดมืด
- **แถบนำเข้าข้อมูลเดิม**: แสดงเฉพาะเมื่อ localStorage ของเครื่องนี้มี Loan จากเวอร์ชัน 1 และยังไม่เคยนำเข้า (ดูข้อ 9)
- **ฟอร์ม Loan** ใช้ทั้งเพิ่มและแก้ไข: เพื่อน, ของ, วันที่ยืม (ค่าเริ่มต้นวันนี้), กำหนดคืน มีปุ่มยกเลิกการแก้ไข
- **ช่องค้นหา/กรอง** ตามชื่อเพื่อน
- **รายการ Loan** แยกกลุ่ม **เกินกำหนด → ยังไม่คืน → คืนแล้ว** แต่ละกลุ่มเรียงตามกำหนดคืนใกล้สุดก่อน
- แต่ละรายการแสดง: ชื่อของ, ชื่อเพื่อน, วันที่ยืม, กำหนดคืน, สถานะ, จำนวนวันเกินกำหนด (เฉพาะเกินกำหนด), วันที่คืนจริง (เฉพาะคืนแล้ว)
- ปุ่มต่อรายการ: **คืนแล้ว** (เฉพาะที่ยังไม่คืน), **ยกเลิกการคืน** (เฉพาะที่คืนแล้ว), **แก้ไข**
- ไม่มีปุ่มลบ

**สถานะการโหลด/บันทึก**
- ระหว่างโหลดรายการแสดง "กำลังโหลด..."
- ระหว่างบันทึก ปิดปุ่มที่เกี่ยวข้องชั่วคราว กันกดซ้ำ
- อัปเดตหน้าจอหลังฐานข้อมูลตอบสำเร็จเท่านั้น (ไม่อัปเดตล่วงหน้า)
- บันทึกไม่สำเร็จ: แสดงข้อความภาษาไทย และคงค่าที่กรอกในฟอร์มไว้

**การแสดงผล**
- ภาษาไทยทั้งหมด, วันที่ พ.ศ.
- รองรับหน้าจอมือถือ (layout ยืดหยุ่น)
- โหมดมืด สลับได้ด้วยปุ่ม และจำค่าไว้ใน localStorage ของเครื่อง (ไม่เก็บใน Supabase)
- ใช้สี/ข้อความแยกสถานะให้ชัด ไม่พึ่งสีอย่างเดียว

## 8. โครงสร้างโค้ด (ระดับโฟลเดอร์)

แยกตรรกะล้วนออกจาก UI และแยกการเรียก Supabase ไว้ชั้นเดียว

```
borrow-buddy/
├── CONTEXT.md
├── design.md
├── .env.example             # ชื่อตัวแปร Supabase (ไม่มีค่าจริง)
├── supabase/
│   └── migrations/          # SQL สร้างตาราง loans, constraint, trigger, RLS
└── src/
    ├── main.jsx
    ├── App.jsx              # ติดตาม session, สลับหน้าเข้าสู่ระบบ/หน้าหลัก
    ├── components/
    │   ├── LoginForm.jsx     # ใหม่
    │   ├── AppHeader.jsx     # ใหม่: อีเมล, ออกจากระบบ, ThemeToggle
    │   ├── ImportBanner.jsx  # ใหม่: นำเข้าข้อมูลเดิม
    │   ├── LoanForm.jsx
    │   ├── LoanList.jsx
    │   ├── LoanItem.jsx
    │   ├── SearchBox.jsx
    │   └── ThemeToggle.jsx
    └── lib/
        ├── loanRules.js      # เดิม ไม่เปลี่ยน
        ├── dateFormat.js     # เดิม ไม่เปลี่ยน
        ├── loanMapper.js     # ใหม่: แปลงแถวฐานข้อมูล ↔ Loan (ฟังก์ชันล้วน)
        ├── supabaseClient.js # ใหม่: สร้าง client จาก env
        ├── authService.js    # ใหม่: signIn, signOut, getSession, onAuthChange
        ├── loanRepo.js       # ใหม่: list, create, update ของ Loan (ไม่มี delete)
        ├── legacyImport.js   # ใหม่: อ่าน Loan เวอร์ชัน 1 และแปลงเป็นข้อมูลนำเข้า
        └── storage.js        # อ่านข้อมูลเวอร์ชัน 1 และธงนำเข้า (saveLoans เดิมคงไว้ให้เทสต์เดิมผ่าน แต่แอปไม่เรียก)
```

- ฟังก์ชันใน `lib/` ที่เป็นตรรกะรับ "วันนี้" เป็นพารามิเตอร์ ไม่เรียกเวลาจริงข้างใน
- คอมโพเนนต์ไม่เรียก `supabase` ตรงๆ ต้องผ่าน `authService.js` / `loanRepo.js`

## 9. การเก็บข้อมูลและการนำเข้าข้อมูลเดิม

**Supabase**
- โหลด Loan ทั้งหมดของเจ้าของหลังเข้าสู่ระบบ (RLS กรองให้อยู่แล้ว แต่ใส่เงื่อนไข `owner_id` ซ้ำในคำสั่งอ่านเพื่อให้ใช้ดัชนีได้ ตามคำแนะนำของ Supabase)
- เพิ่ม/แก้ไข/กดคืน/ยกเลิกการคืน เขียนลงฐานข้อมูลทันทีทีละ Loan
- ใช้คำสั่ง `update` ระบุ `id` ห้ามใช้ `upsert` ที่อาจเขียนทับ

**นำเข้าข้อมูลเดิม (ครั้งเดียวต่อเครื่อง)**
- เงื่อนไขแสดงแถบ: localStorage มีคีย์ Loan ของเวอร์ชัน 1 ที่อ่านได้และไม่ว่าง และยังไม่มีธงนำเข้าของบัญชีนี้
- แถบบอกจำนวน Loan ที่พบ มีปุ่ม **นำเข้า** และ **ไม่นำเข้า**
- นำเข้า: ตรวจทุก Loan ด้วยกติกาข้อ 5 ก่อน, ไม่ส่ง `id` เดิม (ให้ฐานข้อมูลสร้างใหม่), ส่งเป็นชุดเดียว (insert หลายแถวในคำขอเดียว) สำเร็จทั้งหมดหรือไม่สำเร็จทั้งหมด
- Loan ที่ผิดกติกา: ไม่นำเข้า และแจ้งจำนวนพร้อมรายการที่ข้าม
- สำเร็จ: ตั้งธงนำเข้าใน localStorage (ผูกกับ id ของเจ้าของ) เพื่อไม่ให้นำเข้าซ้ำ
- **ไม่ลบ** ข้อมูลเวอร์ชัน 1 ออกจาก localStorage (เก็บไว้เป็นสำรอง)
- JSON ใน localStorage เสีย: ไม่แสดงแถบนำเข้า และไม่แตะข้อมูลเดิม
- กด "ไม่นำเข้า": ตั้งธงเหมือนกัน แถบไม่ขึ้นอีก

**ข้อผิดพลาด**
- เครือข่ายล่มหรือ Supabase ตอบผิดพลาด: แสดงข้อความภาษาไทย ไม่แสดงรายละเอียดทางเทคนิคในหน้าเว็บ
- โหลดรายการไม่สำเร็จ: แสดงข้อความพร้อมปุ่ม "ลองใหม่" ห้ามแสดงรายการว่างเหมือนไม่มีข้อมูล
- session หมดอายุระหว่างใช้งาน (ได้ 401): กลับไปหน้าเข้าสู่ระบบ

## 10. การทดสอบ

**Vitest (ตรรกะใน `lib/` ไม่ต่อ Supabase จริง)**
- เทสต์เดิมของ `loanRules.js` และ `dateFormat.js` ต้องผ่านทั้งหมดโดยไม่แก้
- `loanMapper.js`: แปลงไป-กลับครบทุกฟิลด์, `returned_date` null, ไม่ส่ง `owner_id`/`id` ตอนสร้าง
- `legacyImport.js`: ข้อมูลปกติ, JSON เสีย, อาร์เรย์ว่าง, Loan ผิดกติกาถูกคัดออก, ตัด `id` เดิม, ธงนำเข้าแยกตามเจ้าของ
- `loanRepo.js` / `authService.js`: ทดสอบด้วย client จำลอง (mock) ว่าเรียกตาราง/เมธอดถูก และไม่มีฟังก์ชันลบ

**ตรวจฐานข้อมูล (ด้วยมือหรือ SQL)**
- เจ้าของ A มองไม่เห็น/แก้ไม่ได้ Loan ของเจ้าของ B
- `anon` อ่านตารางไม่ได้
- คำสั่ง `delete` ถูกปฏิเสธ
- insert ที่ `due_date` ก่อน `borrowed_date` ถูกปฏิเสธ
- สมัครสมาชิกผ่าน API ถูกปฏิเสธ
- Supabase security advisors ไม่มีคำเตือน

## 11. ประเด็นที่ตั้งใจไม่ทำ
สมัครสมาชิกเอง, รีเซ็ตรหัสผ่านผ่านหน้าเว็บ, ลบ Loan, แจ้งเตือน, เลื่อนกำหนด, รูปภาพ, ทะเบียนเพื่อน/ของ, ออฟไลน์, เรียลไทม์ หากต้องการในอนาคตให้ทบทวน CONTEXT.md ก่อน
