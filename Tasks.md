# Borrow Buddy: Tasks

อ้างอิง [CONTEXT.md](./CONTEXT.md) และ [design.md](./design.md) ทำตามลำดับ ติ๊กเมื่อเสร็จ

กติกาการทำงาน: JavaScript เท่านั้น, ห้ามลบไฟล์โดยไม่ถามก่อน, ตอนตั้งค่า Vite/React/Vitest ให้ตรวจเอกสารล่าสุดผ่าน Context7 ก่อน

## เฟส 1: ตั้งโปรเจ็กต์

- [x] T1.1 สร้างโปรเจ็กต์ Vite + React (เทมเพลต JavaScript) ในโฟลเดอร์ `borrow-buddy` โดยไม่ทับ `CONTEXT.md` และ `design.md`
- [x] T1.2 ติดตั้ง Vitest และเพิ่มสคริปต์ `test` ใน `package.json`
- [x] T1.3 สร้างโครงโฟลเดอร์ `src/components` และ `src/lib` ตาม design.md ข้อ 7

- **เสร็จเมื่อ:** `npm run dev` เปิดหน้าเปล่าได้ และ `npm test` รันได้

## เฟส 2: ตรรกะ (`src/lib`) ทำแบบเขียนเทสต์ก่อน

- [x] T2.1 `dateFormat.js`: แปลง `YYYY-MM-DD` เป็น พ.ศ. แบบ "24 ก.ย. 2569" + เทสต์
- [x] T2.2 `loanRules.js`: คำนวณสถานะ (ยังไม่คืน / เกินกำหนด / คืนแล้ว) รับ "วันนี้" เป็นพารามิเตอร์ + เทสต์ขอบ (วันนี้ = กำหนดคืนต้องยังไม่เกิน, วันถัดไปเกิน)
- [x] T2.3 `loanRules.js`: จำนวนวันเกินกำหนด + เทสต์
- [x] T2.4 `loanRules.js`: ตรวจความถูกต้องของ Loan (ชื่อเพื่อน/ของห้ามว่าง, กำหนดคืนและวันที่คืนจริงต้องไม่ก่อนวันที่ยืม) คืนข้อความผิดพลาดภาษาไทย + เทสต์ทุกกรณีผิด
- [x] T2.5 `loanRules.js`: จัดกลุ่ม เกินกำหนด → ยังไม่คืน → คืนแล้ว และเรียงตามกำหนดคืนใกล้สุดในแต่ละกลุ่ม + เทสต์
- [x] T2.6 `loanRules.js`: กรองตามชื่อเพื่อน + เทสต์
- [x] T2.7 `loanRules.js`: กดคืนแล้ว (ค่าเริ่มต้นวันนี้ แก้ย้อนหลังได้) และยกเลิกการคืน + เทสต์
- [x] T2.8 `storage.js`: อ่าน/เขียน Loan ใน localStorage คีย์เดียว, JSON เสียหรืออ่านไม่ได้ให้คืนรายการว่างพร้อมสัญญาณแจ้งเตือน ไม่เขียนทับข้อมูลเดิม

- **เสร็จเมื่อ:** `npm test` ผ่านทั้งหมด

## เฟส 3: UI

- [x] T3.1 `App.jsx`: เก็บ state Loan โหลดจาก storage ตอนเปิด บันทึกทุกครั้งที่เปลี่ยน และแสดงข้อความภาษาไทยเมื่ออ่านข้อมูลไม่ได้
- [x] T3.2 `LoanForm.jsx`: ฟอร์มเดียวใช้เพิ่มและแก้ไข (เพื่อน, ของ, วันที่ยืม ค่าเริ่มต้นวันนี้, กำหนดคืน) แสดงข้อผิดพลาดจากการตรวจ มีปุ่มยกเลิกการแก้ไข
- [x] T3.3 `SearchBox.jsx`: ช่องค้นหาตามชื่อเพื่อน
- [x] T3.4 `LoanList.jsx` และ `LoanItem.jsx`: แสดงกลุ่มและรายการตาม design.md ข้อ 6 (วันที่ พ.ศ., จำนวนวันเกินเฉพาะเกินกำหนด, วันที่คืนจริงเฉพาะคืนแล้ว)
- [x] T3.5 ปุ่มต่อรายการ: คืนแล้ว (เลือกวันที่ได้), ยกเลิกการคืน, แก้ไข ไม่มีปุ่มลบ
- [x] T3.6 `ThemeToggle.jsx`: โหมดมืด สลับได้และจำค่าไว้
- [x] T3.7 สไตล์: รองรับมือถือ, สถานะแยกด้วยข้อความและสี (ไม่พึ่งสีอย่างเดียว)

- **เสร็จเมื่อ:** ใช้งานครบทุกอย่างในขอบเขตผ่านหน้าเว็บได้

## เฟส 4: ตรวจรับ

- [x] T4.1 รัน `npm test` ผ่านทั้งหมด
- [x] T4.2 ทดลองในเบราว์เซอร์: เพิ่ม → แก้ไข → กดคืน → ยกเลิกคืน → รีเฟรชแล้วข้อมูลยังอยู่
- [x] T4.3 ทดลองกรณีขอบ: กำหนดคืนวันนี้, กำหนดคืนเมื่อวาน, กรอกวันที่ผิดกติกา, ข้อมูลใน localStorage เสีย
- [x] T4.4 ตรวจหน้าจอมือถือและโหมดมืด
- [x] T4.5 ตรวจว่าไม่มีฟีเจอร์นอกขอบเขต (ลบ, แจ้งเตือน, เลื่อนกำหนด, รูปภาพ, สำรองข้อมูล) และศัพท์ในหน้าเว็บตรงกับ CONTEXT.md

## เฟส 5: เวอร์ชัน 2 (Supabase + เข้าสู่ระบบ)

อ้างอิง design.md เวอร์ชัน 2 ตอนติดตั้ง/ใช้ `@supabase/supabase-js` และตั้งค่า Auth/RLS ให้ตรวจเอกสารล่าสุด (Context7 หรือ Supabase docs) ก่อน

**ฐานข้อมูลและ Auth**
- [ ] T5.1 ปิดการสมัครสมาชิกเองใน Supabase Auth และสร้างบัญชีเจ้าของใน Dashboard (ยืนยันอีเมลแล้ว)
- [x] T5.2 เขียน migration ใน `supabase/migrations/`: ตาราง `loans` ตาม design.md ข้อ 4 (คอลัมน์, check constraint, ดัชนี `owner_id`, trigger `updated_at`)
- [x] T5.3 เพิ่มใน migration: เปิด RLS, policy `select`/`insert`/`update` สำหรับ `authenticated` ตาม design.md ข้อ 6, ไม่มี policy `delete` และ revoke delete
- [ ] T5.4 apply migration แล้วตรวจ security advisors ต้องไม่มีคำเตือน

**ตั้งค่าฝั่งเว็บ**
- [x] T5.5 ติดตั้ง `@supabase/supabase-js`, สร้าง `.env.example`, ตรวจว่า `.env.local` อยู่ใน `.gitignore`
- [x] T5.6 `supabaseClient.js`: สร้าง client จาก env แจ้งข้อความภาษาไทยถ้าไม่มีค่า env

**ตรรกะ (`src/lib`) เขียนเทสต์ก่อน**
- [x] T5.7 `loanMapper.js`: แปลงแถว ↔ Loan + เทสต์ (ครบทุกฟิลด์, `returned_date` null, ไม่ส่ง `id`/`owner_id` ตอนสร้าง)
- [x] T5.8 `authService.js`: signIn, signOut, getSession, onAuthChange แปลงข้อผิดพลาดเป็นข้อความไทย + เทสต์ด้วย client จำลอง
- [x] T5.9 `loanRepo.js`: list, create, update (ไม่มี delete, ไม่ใช้ upsert) แปลงข้อผิดพลาดเป็นข้อความไทย + เทสต์ด้วย client จำลอง
- [x] T5.10 `legacyImport.js` และปรับ `storage.js`: อ่าน Loan เวอร์ชัน 1, คัด Loan ผิดกติกา, ตัด `id` เดิม, ธงนำเข้าแยกตามเจ้าของ, ไม่ลบข้อมูลเดิม + เทสต์

**UI**
- [x] T5.11 `App.jsx`: ติดตาม session สลับหน้าเข้าสู่ระบบ/หน้าหลัก, ล้าง Loan เมื่อออกจากระบบหรือ session หมดอายุ, โหลด Loan จาก `loanRepo` แทน localStorage
- [x] T5.12 `LoginForm.jsx`: อีเมล, รหัสผ่าน, กันกดซ้ำ, ข้อความผิดพลาดไม่บอกว่าผิดช่องไหน ไม่มีลิงก์สมัครสมาชิก
- [x] T5.13 `AppHeader.jsx`: อีเมลเจ้าของ, ปุ่มออกจากระบบ, ThemeToggle
- [x] T5.14 ปรับเพิ่ม/แก้ไข/กดคืน/ยกเลิกการคืน ให้เขียนผ่าน `loanRepo` อัปเดตหน้าจอหลังสำเร็จ, กันกดซ้ำ, คงค่าฟอร์มเมื่อบันทึกไม่สำเร็จ
- [x] T5.15 สถานะ "กำลังโหลด..." และข้อความโหลดไม่สำเร็จพร้อมปุ่ม "ลองใหม่"
- [x] T5.16 `ImportBanner.jsx`: แสดงตามเงื่อนไข design.md ข้อ 9, ปุ่มนำเข้า/ไม่นำเข้า, แจ้งจำนวนที่นำเข้าและที่ข้าม

**ตรวจรับ**
- [x] T5.17 `npm test` ผ่านทั้งหมด (รวมเทสต์เดิมโดยไม่แก้)
- [ ] T5.18 ทดลองในเบราว์เซอร์: เข้าสู่ระบบ → เพิ่ม → แก้ไข → กดคืน → ยกเลิกคืน → รีเฟรชแล้วยังอยู่ → ออกจากระบบแล้วไม่เห็นข้อมูล → เข้าจากอีกเบราว์เซอร์เห็นข้อมูลเดียวกัน
- [ ] T5.19 ทดลองนำเข้าข้อมูลเดิม: มีข้อมูลปกติ, มี Loan ผิดกติกา, JSON เสีย, กดไม่นำเข้า, รีเฟรชแล้วแถบไม่ขึ้นซ้ำ, ข้อมูลใน localStorage ยังอยู่
- [ ] T5.20 ตรวจสิทธิ์ตาม design.md ข้อ 10: เจ้าของ A ไม่เห็น Loan ของ B, `anon` อ่านไม่ได้, delete ถูกปฏิเสธ, สมัครสมาชิกผ่าน API ถูกปฏิเสธ
- [ ] T5.21 ทดลองกรณีผิดพลาด: รหัสผ่านผิด, เครือข่ายล่มตอนบันทึก, วันที่ผิดกติกา
- [ ] T5.22 ตรวจว่าไม่มีฟีเจอร์นอกขอบเขต (สมัครสมาชิก, ลบ, แจ้งเตือน, เลื่อนกำหนด, รูปภาพ), ไม่มี service role key หรือค่า env จริงใน repo และศัพท์ในหน้าเว็บตรงกับ CONTEXT.md

- **เสร็จเมื่อ:** ทุกข้อด้านบนผ่าน และข้อมูลทั้งหมดอยู่ใน Supabase โดยเจ้าของเห็นเฉพาะ Loan ของตัวเอง
